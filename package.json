{
  "name": "catalyst-crew-vercel-deploy",
  "private": true,
  "scripts": {
    "build": "cd frontend && bun install && bun run build && cd .. && rm -rf .vercel && cp -R frontend/.vercel .vercel"
  }
}
